#include "bits/stdc++.h"
using namespace std;
#define fast                                                                   \
  ios_base::sync_with_stdio(false);                                            \
  cin.tie(NULL);
typedef long long ll;

void sol() {
  ll n, k;
  cin >> n >> k;
  string grid[k];
  for (ll i = 0; i < k; i++) {
    string temp;
    cin >> temp;
    grid[i] = temp;
  }
  for (ll &i : arr) {
  }
}

int main() {
  fast;
  ll q;
  cin >> q;
  while (q--) {
    sol();
    cout << "\n";
  }
  return 0;
}
